import Phaser from '../lib/phaser.js';

export default class Menu extends Phaser.Scene {
    constructor() {
        super('Menu')
    }
    preload() {
        this.load.image('', '')
    }
    create() {

    }
}